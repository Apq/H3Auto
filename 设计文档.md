# 打铁助手（H3Auto）· 设计文档

> 本文档是「打铁助手」插件的设计与实现说明，以实际代码为准（`modules/*.inc.cpp`）。
> 设计方案的完整讨论见 `H3Note/H3Auto行动与目标策略实现方案.md`，本文档是其在代码仓库内的落地版。

---

## 1. 项目目标与定位

打铁助手（工程名 H3Auto）是一个英雄无敌3（SoD / HD Mod）打铁助手 DLL 插件。核心定位：

- **辅助人类玩家，减少重复性操作。** 在漫长战斗中代替玩家发出重复的游戏操作。
- **两种实现途径**：直接调用原版函数，或模拟玩家操作（点击 / 按键）。
- **只辅助人类玩家，不影响电脑阵营。** 回放、隐藏战斗、AI 回合一律不接管。

### 1.1 核心原则

1. **不接管全部决策。** 只在玩家预设了策略的部队上代为发出操作，其余保持手动。
2. **技能条件化。** 战争机器的可控性取决于英雄是否拥有对应技能（见 §2）。
3. **降级安全。** 配置的行动无法实现时，按玩家意愿降级为防御，或转回手动（见 §3.6）。
4. **只辅助人类。** 电脑阵营、回放、隐藏战斗一律不接管。
5. **配置仅驻留内存。** 5 套方案存进程内存（每编号一份草稿，切换编号不丢），不写回 INI（INI 仅提供 UI 标签、热键等静态配置）。上次存/读档编号存独立文件 `H3Auto.last`。

### 1.2 不负责的范围

- 敌方部队 / 电脑阵营的策略。
- 战斗 AI 逻辑（由游戏原版处理）。
- 生物信息窗口扩展、战斗价值显示等非自动化功能。

---

## 2. 战争机器的条件化控制

战争机器平时由电脑自动操作（很笨）。插件按机器类型决定是否进面板，以及「手动」是否可选（仍不提供原版之外的直接操控）：

| 战争机器 | 开放设置的条件 | 可设置的行动 |
| ---- | -------- | -------- |
| 弩车  | 始终可进面板 | **有炮术**：手动（默认）/ 远程攻击；**无炮术**：仅远程攻击 |
| 箭塔  | 始终可进面板 | 同弩车 |
| 急救帐篷 | 始终可进面板 | **有急救术**：手动（默认）/ 急救治疗；**无急救术**：仅急救治疗 |
| 投石车 | 英雄有**弹道术** | **手动 / 防御**（不做攻城目标自动化） |
| 弹药车 | **永不进面板** | 不接管（始终原版） |

> 弩车/箭塔：炮术决定是否允许「手动」；无炮术时强制自动远程。
> 急救帐篷：急救术决定是否允许「手动」；无急救术时强制自动急救。
> 投石车：无弹道术则不出现在面板。

英雄技能查询：`HeroHasSkill`（弹道术 10 / 炮术 20 / 急救术 27）。

---

## 3. 需要提供的辅助能力

### 3.1 部队行动前英雄自动施法（快捷施法 / 循环施法）

- 每支部队行动前，英雄可先自动施放一个快捷法术。
- 由于每回合想施放的法术可能不同，支持**循环施法**：按回合轮换施放快捷施法的槽位。
- 循环施法只通过**快捷施法**功能实现（快捷键 1-9/0）。UI 单行最多 **10 个**快捷键槽位（`spellSlots[10]`），运行游标保存在本场部队跟踪表；原版快捷施法仍有 10 个键可选。

### 3.1.1 首动保活

和循环施法不是一回事：循环施法绑在**某支部队行动前**；本条绑在**每回合第一次把控制权交给玩家时**（含本插件接管）。目的：**防止队列中的部队死光**。

- **部队级只有一个选项**：卡片底部 `加入保活队列 [✓]` 复选框（随方案与槽位规则一起存储、重排、提交）。不勾即不参与。
- **保活策略（方案级）**：何时施救，由方案行下方的下拉框选择（随 5 套方案各自保存、切换）：
  - **无**（默认）：本方案不做保活；
  - **部队全灭后**：只救已全灭且仍保留尸体者；没有全灭者本回合不施法；
  - **回合内首动**：队列中任何部队只要有损失（已损 HP > 0）就救，最激进；
  - **损失量大于恢复量**：已损血量（`死亡数 × 单兵满血 + 顶层已损 HP`，全灭即满损失）**严格大于**一次可恢复量才救。可恢复量 = 施一次复活/聚灵能拉回的总 HP（复活/聚灵 基础 50×力量、高级 75×力量、专家 100×力量，以逆向结论为准）。
- **判定时机**：「回合内首动」每回合只判一次（判过即记）；「部队全灭后」「损失量大于恢复量」为事件型，**每次行动（控制权交给玩家）都判**。游戏一回合只允许施法一次：已施法/法力不足时静默跳过，不记回合标记，之后的行动继续判。
- 够格者中救谁：统一取**当前剩余总血量最低**的一支（全灭者剩余 0 天然最前）。
- 法术由插件自动选，与快捷施法完全无关，不占快捷施法槽：目标部队是亡灵（`undead` 标志位）→ 聚灵（`eSpell::ANIMATE_DEAD = 39`）；活体 → 复活（`eSpell::RESURRECTION = 38`）。对应法术没学（`learnedSpells` / `GetSpellExpertise = 0`）则该部队本回合保不了。
- 施法前提：法力足够、本回合英雄未施过法（`hero_casted`）。执行：插件直接调 `H3CombatManager::CastSpell(spell_id, 目标hex, cast_type, -1, 法术等级, 力量)`（SoD `0x5A0140`），目标格取该部队所在格。施法动画与 `hero_casted` 置位由游戏内部完成。
- 每回合只判一次；按方案策略（§3.1.1）在够格部队中选目标。全手动、单次接管时不判、不施。失败不挡部队动作和循环施法。

### 3.1.2 自动停止（敌方预计回合）

打铁跑着时，按最近两笔取样之间的掉血速度估计敌方还要几回合全灭。预计回合数不大于玩家设的阈值时，切到全手动并把控制权交回玩家。

- **阈值（方案级）**：保活策略行最右侧 `停止: [数字]`，范围 0..999，默认 10。0 = 关闭。随 5 套方案各自保存、切换、提交。
- **血量**：敌方每个存活槽的 `存活数 × 单兵满血 − 顶层已损`。箭塔、弩车、急救帐篷、投石车、弹药车不计入。
- **取样**：每个战斗回合只记一次，只保留最近两笔。两笔之间掉血为 0 或敌方总血量变高（召唤、复活）时，丢掉旧笔、从当前重新计，不判。
- **判定**：最近两笔 `经过回合 > 0` 且 `掉血 > 0` 时，预计剩余回合 = `当前血量 × 经过回合 / 掉血`（向上取整）。`预计 <= 阈值` 才停。不足两笔、或阈值是 0，不停。
- **停的方式**：控制权切到全手动（`g_control = CM_MANUAL`，与按启停热键切到停相同），清掉未完成的单次接管。当前这次行动不再代发。之后玩家可再按热键启动。
- 全手动、设置面板打开时不取样、不判。重打或战斗状态重置后两笔取样作废。

### 3.2 自动防御

最简单的行动：本回合原地防御（`BA_DEFEND`）。

### 3.3 自动射击（远程攻击）

射手部队自动选择敌方目标射击。目标选择器支持：随机、远程速度优先、数量最多。

### 3.4 循环近战

- `AA_MELEE_ATTACK` 的枚举值固定为 `4`，显示名改为「循环近战」。
- 每支部队最多设置 10 组 **站立位＋攻击位**；执行时按游标逐组循环，只有动作成功提交后才推进游标。
- 「＋」用于追加组合；已有组合显示为 `A01→A02`，点击任意组合可连续重选并覆盖该记录。
- 每组采用一次连续战场拾取：先点**站立格**，插件立即用原版 `CCellShd` 蓝色格子在战场临时标示；再点与其不同且相邻的**攻击目标格**。第二格相同或不相邻时忽略并继续拾取。
- 第二格成功、右键取消或面板关闭时，临时标示随战场重绘撤销；整个拾取过程点击均由 BattleUI 输入屏障吞掉，不会触发真实部队行动。

### 3.5 循环移动

- 玩家设置一个位置列表（最多 16 个点），部队按回合在这些点之间巡逻循环移动。
- 位置通过**点击＋追加**方式设置（见 §8.4）：点序列末尾「＋」或已有点进入战场拾取 1 格，写回对应槽（最多 16）；右键取消不改原记录。

### 3.6 降级为自动防御

- 当设置的行动方式无法实现时（如循环近战当前组合的目标不存在、移动路径被阻挡），若玩家勾选「允许降级」，则自动改为防御。
- 若玩家**未**勾选允许降级，则配置动作失败后停止插件重试并交回玩家操作。
- 战争机器交回玩家还须具备对应技能：投石车需要弹道术，弩车/箭塔需要炮术，急救帐篷需要急救术；无对应技能时保持原有分支行为。

---

## 4. 数据结构（源：`modules/ConfigLog.inc.cpp`）

### 4.1 枚举

```cpp
enum AutoActionKind {         // 行动类型
    AA_MANUAL = 0,            // 手动（不接管）
    AA_DEFEND = 1,            // 防御
    AA_WAIT = 2,              // 等待（保留占位，UI 已隐藏）
    AA_MOVE = 3,              // 循环移动
    AA_MELEE_ATTACK = 4,      // 循环近战（枚举值保持不变）
    AA_RANGED_ATTACK = 5,     // 远程攻击
    AA_FIRST_AID = 6,         // 急救
};

enum AutoTargetSide {         // 目标阵营
    ATS_OWN = 0,              // 己方
    ATS_ENEMY = 1,            // 敌方
    ATS_EITHER = 2,           // 双方
};

enum AutoTargetSelector {     // 按行动分流
    SEL_RANDOM = 0,           // 随机（远程/急救）
    SEL_RANGED_SPEED = 1,     // 远程：远程速度优先
    SEL_COUNT_HIGH = 2,       // 远程：数量最多
    SEL_WOUND_RATIO = 3,      // 急救：失血比例最高
    SEL_WOUND_VALUE = 4,      // 急救：失血数值最大
};
```

> INI `[Actions]/[Selectors]` 段和默认标签数组按枚举下标排列。目标阵营下拉已停用，阵营由各行动固定。

### 4.2 规则结构

```cpp
struct AutoTargetRule {
    AutoTargetKind   kind;             // 目标类型（无 / 部队 / 位置）
    AutoTargetSide   side;             // 目标阵营
    AutoTargetSelector selector;       // 选择器
    int16_t          meleeStandHex;    // 旧版单组近战兼容镜像
    int16_t          meleeAttackHex;   // 旧版单组近战兼容镜像
    int16_t          moveWaypoints[16]; // 循环移动: 路径点列表（最多 16）
    int8_t           moveWaypointCount;  // 有效路径点数
    int16_t          meleeStandHexes[10]; // 循环近战: 各组站立位
    int16_t          meleeAttackHexes[10];// 循环近战: 各组攻击位
    int8_t           meleePairCount;     // 有效组合数
};

struct AutoStackRule {
    AutoActionKind  action;              // 行动类型
    AutoTargetRule  target;              // 目标规则
    bool            allowDefendFallback; // 无法执行时是否降级为防御
    bool            quickCastFirst;      // 兼容：spellSlotCount>0 时为 true
    int8_t          spellSlot;           // 兼容镜像：等于 spellSlots[0]（1-9/0）
    int8_t          spellSlots[10];      // 循环施法快捷键序列（单行最多 10 槽）
    int8_t          spellSlotCount;      // 有效槽位数 0..10

    // 首动保活（§3.1.1）：挂单支部队，随方案/槽位一起存储与重排。
    uint8_t         protectEnable;       // 0=关闭；1=加入保活队列（策略全局唯一）
};
```

### 4.3 方案存储

```cpp
AutoStackRule g_profiles[5][21];      // 5 套方案草稿 × 21 槽位，仅驻留内存（切换编号不丢）
int          g_active_profile;       // 当前生效方案编号（勾号时 = 选中编号）
AutoStackRule g_active_rules[21];     // 生效方案运行时视图 = g_profiles[g_active_profile]
int          g_last_profile;        // 上次存/读档槽位（独立文件 H3Auto.last 持久化）
AutoStackRule g_active_rules[21];     // 当前生效规则（执行端读取；首动保活字段随槽位规则走）
uint8_t      g_protect_strategy[5];  // 方案级保活策略（ProtectStrategy，默认 PS_NONE）
uint16_t     g_stop_turns[5];        // 方案级自动停止阈值（0..999，默认 10，0=关闭）
```

### 4.4 当前战斗部队跟踪

玩家提交设置后，执行端会绑定本场人类侧部队跟踪表（`g_stack_track[21]`）：

- **配置身份**：普通开战部队使用 `side + source_army_slot`；战争机器使用“类型 + 同类序号”。
- **当前定位**：`side + army_slot_ix` 只表示本轮战斗中的数组槽位；`_BattleStack_` 指针也只表示当前数组单元，不能跨重打使用。
- **运行刷新**：存活（`count_current>0`）、位置（`hex_ix`）、数量（`count_current`）
- **执行前置**：只有活动单位通过跟踪校验时才套用 `g_active_rules[slot]`
- **动态部队**：召唤物和克隆物有本场内身份（`STACK_ID_SUMMON`：类型 + 同类出现序号），本场内可配置可自动执行；克隆体先按 `clone_id>0` 判定，避免继承本体的军队槽身份。
- **失效条件**：身份不匹配、死亡后槽位被复用、非人类侧、未绑定

这样避免“只按槽位索引取规则”导致的错套：敌方同槽、死亡后槽复用、同兵种召唤物复用，以及跨侧活动单位。

### 4.5 快速战斗重打的身份重建

原版和 HD 的战斗管理器通常复用同一个 `H3CombatManager` 及其 `stack[2][21]` 固定数组。取消快速战斗后，物理地址可能不变，但数组单元中的逻辑部队会按战前军队快照重新初始化。因此指针相同不代表仍是同一支逻辑部队。

取消重打回到 BattleUI 时，`EnsureStackTrackingBound` 执行以下顺序：

1. 扫描新战场并生成稳定身份。
2. 将 5 套方案从旧战场槽重排到新战场槽；找不到对应身份的槽使用默认规则。首动保活勾选（`protectEnable`）在 `AutoStackRule` 内，随槽位规则整体一起重排；保活策略是方案级不参与重排（`g_protect_strategy[5]`），不参与重排。
3. 保留方案，清除旧的活动部队指针、施法等待、动作唤醒和单次接管状态。
4. 重置本轮移动、近战、施法游标，递增战斗尝试代次。
5. 以新战场对象重新建立 `g_stack_track[21]`。

接受结果才调用 `OnBattleResultAccepted`，清除 5 套方案；取消重打不会清除方案（`g_last_profile` 也不清，跨战斗保留）。

稳定身份的适用范围：

- `source_army_slot` 适用于原英雄/军队中的 0 到 6 号开战部队。
- 战争机器没有通用原军队槽，使用类型和同类序号。
- 召唤物、克隆物以及其他 `source_army_slot=-1` 的衍生对象没有跨重打身份：本场内按 `STACK_ID_SUMMON` 跟踪执行，重排（`BuildStableStackSlotRemap`）明确跳过该类身份，重打后回到默认规则。

---

## 5. 行动可选集（源：`CellControl_GetAllowedActions`）

按部队类型返回允许的行动列表（决定行动下拉显示哪些项）：

- **普通近战部队**：手动、防御、循环移动、循环近战。
- **射手部队**（`is_ranged`）：手动、防御、循环移动、循环近战、远程攻击。
- **战争机器**：按 §2 返回行动下拉选项（有技能则含「手动」；无技能则仅该机器唯一自动行动）。**不是**启用/禁用开关。

> `is_ranged` 由面板打开时按 `stack.info.shooter` 精确判定并存入 `CellData.is_ranged`；切换方案时复用，不用 `creature_type` 粗判（否则射手如大法师丢失远程选项）。

---

## 6. 目标可选集（源：`CellControl_GetAllowedSelectors`）

按行动类型返回允许的选择器（决定选择器下拉显示哪些项）：

- **远程攻击**：**随机 / 远程飞兵高速优先 / 数量最多**。排序依次比较远程、飞兵、速度，平分保留先出现者。
- **急救治疗**：**失血数值 / 失血比例 / 随机**（候选仅己方伤员；默认失血数值）。
- **循环近战 / 循环移动**：不显示选择器菜单。
- **投石车**：无独立行动枚举；UI 仅手动 / 防御。

---

## 7. 规则规范化（源：`CellControl_NormalizeRule`）

行动变化或加载方案时调用，保证规则自洽：

1. 行动改变时，若当前选择器不在允许集，吸附到允许集首项。
2. 循环近战：压紧并校验 `meleeStandHexes/meleeAttackHexes`，每组必须为相邻格；旧版单组字段首次加载时迁移为序列第 1 组。
3. 循环移动：确保 `moveWaypoints` 已初始化，`kind=AT_POSITION`。
4. 切换到无目标的行动（防御 / 等待）时，清空目标相关字段。

---

## 8. 设置面板 UI（源：`modules/SettingsDlg.inc.cpp` + `CellControl.inc.cpp`）

### 8.0 模态方案：加固伪模态（不改真模态）

**背景**：曾考虑改为原版 `H3Dlg` 真模态窗口，但真模态靠嵌套阻塞消息循环独占输入，与战场拾取（拾取时需战斗界面响应点击）机制冲突——真模态期间战场被冻结，点不动；要拾取只能关窗重开，体验更差。

**结论**：保留当前**伪模态**方案（自绘到 `screenPcx16` + 自建 hook `Hook_BattleMsgProc`），重点加固：

- 拾取期间确保背景战斗界面不响应误点（屏蔽 hover / 点击穿透）。
- 模态遮挡干净（`ForcePanelModalDepth_` 顶模态计数器）。
- 重绘时机正确（拾取进入 / 退出、格子展开 / 收起边界）。

### 8.1 布局：一行一格宽卡片（568×110）

单列纵向排列，每支部队一张宽卡片。卡片保持旧版高度 **110**，金框恢复 **624×342**（表格上方不再留设置空行，保活行移入卡片内，见 §8.7）。超出用滚动条。

**普通部队卡：**

```text
[图标]  行动前循环施法: [1▾][2▾]…＋     （顶部整行，单数字槽，最多 10）
 58×64  数量 12    行动 [远程攻击▾]      选择器 [数量最多▾]
        位置 A07   允许降级 ☑            （近战: 5×2 组合槽 / 移动: 8×2 路径点槽）
```

**战争机器卡**（准入见 §2；弹药车不进面板）：

```text
[图标]  数量 1        行动 [手动/远程/急救/防御▾]   （按机器与技能分支）
 58×64  位置 K08      （无选择器时第三列可空；有急救/远程则显示选择器）
```

战争机器卡（当前实现）：
- **隐藏降级复选框**（`ActionShowsFallback` 对战争机器恒 false）。
- **行动仍是下拉框**（不是启用/禁用开关）。
- **循环施法行仍绘制**（与普通部队同布局；战争机器一般无施法需求，可空着）。
- 位置虽固定但保留显示供参考。

### 8.2 普通部队卡小列布局

- **图标小列**（x=4，58×64）：部队 DEF 图标 + 金框（贴卡片顶部；位置/数量仍贴卡片下边缘）。
- **位置/数量小列**（x=68，宽 74）：数量（右对齐）、位置（贴格子下边缘左对齐）。
- **顶部整行**（y=6）：标签「行动前循环施法:」+ 单行最多 10 个快捷键小槽（已有项可点开重设，末尾「＋」追加；点后弹模态框按 1-9/0 录入）。
- **行动小列**（x=148，宽 202）：行动下拉（中行）+ 允许降级复选框（下行）。
- **第三小列**（x≈192，宽≈368）：选择器下拉，或循环近战 5×2 组合槽，或循环移动 8×2 路径点槽；槽宽按文本宽度定，一行能摆几个就是几个。

### 8.3 下拉展开几何

- 各下拉从其所在小列的行下边缘展开，宽度取所在小列宽（循环施法槽用槽宽）。
- `GetExpandListLeftX / GetExpandListTopY / GetExpandListWidth` 统一计算基准，绘制（`DrawExpandedTo`）、命中（`HitExpandVisibleIndex`）共用。

### 8.4 战场拾取（循环近战 / 移动路径）

统一由透明 BattleUI item 作为输入屏障：从消息中的游戏逻辑坐标调用 `SquareAtCoordinates`（`0x464380`）转换当前 hex，并对点击执行 `StopProcessing()`，不使用坐标下拉，也不让点击穿透到真实战斗操作。

- **循环近战**：点击已有组合或末尾「＋」后隐藏面板；第一击选择站立格并用 `H3CombatManager::ShadeSquare` / `CCellShd` 临时标示，第二击选择与其不同且相邻的攻击目标格。成功后覆盖或追加记录、撤销标示并恢复面板；第二格相同或不相邻时保持拾取；右键取消且不改原记录。
- **循环移动路径**：点「＋」或已有点后进入拾取，只点 1 格写回对应槽（最多 16）；右键取消不改原记录。
- 面板正常打开及拾取期间均保持战场 hover 屏蔽，避免显示待行动部队的移动范围或攻击提示；设置拾取只显示插件自己的单格临时标示。

### 8.5 标签来源（`LoadLabels_`）

INI `[Actions]/[Selectors]` 段提供 UI 标签，严格按枚举下标排列。缺失时用内置默认标签数组兜底。

### 8.6 帮助说明（右上角 `?`）与悬停 tips

- **悬停 tips（方案 A）**：鼠标悬停在提示区时，底部状态栏显示对应说明文字——命中区：标题带（热键行：三键名读配置 + 右键「自动战斗」打开）、读档/存档按钮、方案 1-5 按钮行、保活策略行、停止输入行、规则金框区、勾号/取消按钮。悬停期间每帧刷新 3 秒保持期（按光标位置判定，静止悬停不消失）；移开后 3 秒到期自动清空。提示色为金色（`eTextColor::GOLD`），区别于成功（绿）/失败（红）；结果文字出现时绘制优先于提示，即新状态文字替换提示。
- 设置面板右上角绘制 `?` 按钮。
- 点击后弹出与快捷施法录入同风格的**伪模态**说明框（遮暗面板 + 金框），内容覆盖：打开方式、5 套方案、循环施法/近战/移动、保活、自动停止、读档/存档、右键删除、人工接管热键、设置生命周期。
- 点「关闭」或按 `ESC` 关闭说明；说明打开时吞掉底层点击，不关闭设置面板本身。
- 面板整体居中基础上移 50px（`s_p.y` 计算后减 50，负值截 0）。
- 方案按钮 1-5 = 编号切换：先存回当前编号草稿，面板切到新编号的草稿；内存 5 套草稿独立保留，未存档/未读档的编号是默认空配置，切走再切回不丢。勾号提交全部 5 套，选中编号立即生效。
- `?` 左侧依次是「读档」「存档」（同高 24px，宽 44px）。**方案编号 = 存档文件编号**：每个编号一个独立文件，DLL 同目录 `H3Auto.profiles.1` .. `H3Auto.profiles.5`（前缀相同 + 编号）。存档把选中编号的草稿（含保活策略、停止阈值与当前部队表）写入对应文件；读档从选中编号的文件读草稿并按四轮关联对位，写入选中编号的草稿（其他编号不动）。存/读档成功后把编号写入独立文件 `H3Auto.last`（一行数字 1..5，不写 INI；下次进面板自动选中，不自动读档；无文件默认 1）。两者只动面板草稿，不改已生效方案、不暂停自动执行；读档失败（文件不存在或损坏）草稿保持不变，结果记日志。
- 存档文本格式：一行 `H3AP3` + 21 槽部队表（每槽 2 个整数：生物类型、开战初始数量；空槽 `-1 0`）+ 1 个策略 + 1 个停止回合 + 21 条规则（每条 59 个十进制整数，字段数由常量表达式自校验），共 1283 个整数（每文件一套）。旧版本（3575/3580 交错坏档、6205 无部队表的 H3AP1、6247 整数的 H3AP2 五套档）一律拒绝。
- **读档四轮关联**：存档部队表与当前面板部队按四轮匹配把规则搬进当前部队格子——轮 1「槽位 + 生物类型 + 初始数量」（三项全等零误配，同场掉兵后读档即此）；轮 2「生物类型 + 初始数量」（换了槽、规模没变；数量相等天然消歧，规则跟部队走）；轮 3「槽位 + 生物类型」（没换槽、规模变了；同类型任一边出现多组时降级跳过，交给轮 4）；轮 4「只按生物类型」（兜底，先到先得）。初始数量权威来源为 `H3Hero::army`——战斗中不写回、保持战前值；召唤物/克隆/战争机器退回 `numberAtStart`。每轮跳过两边已匹配槽位并记录存档侧已匹配槽。未匹配的当前部队保留原草稿不动；未匹配的存档规则静默丢弃（部队不在本场 = 忽略，不报错）。关联在草稿摆放中即生效，点勾号后随本场槽位提交，之后场内变化走 §4.5 稳定身份重排。设置界面不显示初始数量。

### 8.7 首动保活（部队勾选 + 策略下拉）

设置面板整体加高一行（22px，480→502），方案按钮行与金框之间空出一行放**保活策略**（全局唯一）；部队卡内只留勾选：

```text
方案行下方： 保活策略: [无 ▼]                    停止: [10]
卡片底部：   [✓] 加入保活队列
```

- 卡片复选框：点击切换勾选，勾=加入保活队列；卡片不再有倍率等其它保活控件。
- 策略下拉：label「保活策略:」+ 下拉框显示当前方案草稿的策略；点击展开 4 项列表（当前项金色、悬停高亮），点项即选中并收起，点别处/ESC 收起；展开时吞掉底层点击、Enter 不提交设置面板；切方案时下拉收起，重新打开显示新方案的策略。
- 金框自 y=72 下移到 y=94（`GRID_Y/SCROLL_Y` 80→102），卡片/滚动条/确定取消按钮随 `PANEL_H` 整体下移。
- 触发时法术自动选（亡灵→聚灵、活体→复活），UI 不出现法术选择。
- 与卡片其余设置同一套草稿/确定提交/取消丢弃；卡片勾选重打时随槽位规则按稳定身份整体重排，策略本身是方案级不参与重排，确定时与 5 套方案一起提交（`CommitProfiles`）。
- 同一行最右侧是自动停止阈值（§3.1.2）：`停止:` + 数字框，默认 10。点击数字框后按数字键录入，再点框外或按 Enter 写回当前方案草稿；ESC 放弃本次录入。0 表示关闭。阈值随方案提交，也随 `H3Auto.profiles` 保存。

---

## 9. 方案保存流程

目标行为：打开界面自动加载上次设置；调整过程中不影响已保存设置；仅点「确定（勾✓）」才真正写入。

- 打开面板：`g_profiles → s_p.draft_rules`，进入草稿副本编辑，原方案不动。
- 编辑格子：改动只写 `s_p.cells[].data.rule`（内存草稿）。
- 切换方案（`SelectProfile_`）：先 `SaveCurrentCellsToDraft_` 存回草稿，再 `LoadSelectedProfileIntoCells_` 载入目标方案。
- 点确定（`CommitAndCloseSettingsPanel_`）：`SaveCurrentCellsToDraft_` → `CommitProfiles` 一次性提交全部 5 套到 `g_profiles`，选中编号立即生效。
- 点取消 / ESC：直接关闭，草稿丢弃。

### 9.1 设置生命周期（锚定「一场战斗」）

设置只对**当前这场战斗**有效，生命周期以「玩家接受战斗结果」为终点，而非战斗动画打完：

1. 设置在面板确定后写入 `g_profiles` / `g_active_rules`，整场（含重打）常驻内存。
2. **重打不算新战斗**：结果窗出现后点 HD「取消/重打」（控件 id `0x1FB`）→ 方案保留；BattleUI 回来后状态机重打边（`BE_RESULT_RETRY`）触发 `EnsureStackTrackingBound` 按稳定身份重排方案、清理本轮运行态并强制重绑跟踪；控制权保持（全手动是玩家显式选择，重打不清）。
3. 反复重打仍是同一场，设置一路保留。
4. **点结果窗「确定/接受」**（控件 id `0x7802` / iOkay）→ 状态机 `BE_RESULT_ACCEPTED` 边：清空 5 套方案 + 运行时状态 + 跟踪，控制权回自动（下一场不残留全手动）。`OnBattleResultAccepted` 保留为兼容外壳，实际清理在边动作里。
5. 无取消按钮的纯原版结果窗：结果窗消失且未点取消 → 视为接受并清除。
6. **兜底边**：战斗 UI 消失且未经过结算（战斗中/结算中读档、退主菜单）→ `BE_BATTLE_UI_GONE` 同样清运行时与跟踪，但 5 套方案保留；取消重打后 UI 重建常超过 3 帧检测窗，重打边置 3000ms 宽限防误触。
7. 实现：每帧检测虚表 `0x0063D46C`（CPResult）的出现/消失与按钮命中（`CheckBattleResultLifecycle_`），生命周期事件汇成 `BE_RESULT_SHOWN / RETRY / ACCEPTED` 三条边交给状态机（§15）。

---

## 10. 执行分发（源：`modules/AutoExecute.inc.cpp`）

### 10.1 接管判定（`DecideTakeover`）

回放/隐藏战斗（`IsHiddenBattle`）一律 `CD_KEEP_ORIGINAL`。战术布阵阶段不接管。

**最高优先级：人工控制门**（控制权子状态 `g_control`，§15.2；不改 5 套方案、不推进任何循环游标）：

| 模式 | 热键（INI `[Hotkeys]`，可改） | 行为 |
| --- | --- | --- |
| 全手动 `CM_MANUAL` | `ToggleManual=F9` 切换 | 本场所有已配置部队都把控制权交回玩家，直到再按一次恢复自动 |
| 单次接管 `CM_ONESHOT_WAIT/LOCKED` | `OneShotManual=J` 边沿触发 | 锁定当前（或下一支）人类侧部队；该部队完成一次**玩家**动作后自动恢复 |
| 打开设置 | `OpenSettings=P` 边沿触发 | 仅战斗中·面板关响应；打开即切全手动并清施法等待（§15.3），与右键自动战斗按钮同一条打开边 |

保存方案（面板「确定」）只落方案并进入**全手动**，不立即自动执行；按 `F9` 启动自动。取消重打/重绑不清除全手动；点结果窗「接受」后控制权回自动，兜底边（UI 消失）同样回自动。

约束：

- 若当前已有插件提交中的施法/近战等“飞行中”动作，单次接管延后到下一支（`CM_ONESHOT_WAIT`），绝不半途打断。
- 热键按下由常驻 WH_KEYBOARD 线程钩子捕获（keydown 边沿、滤自动重复），挂回合控制权判定的轮询只消费标志——战斗动画期间轮询不运行也不漏按。战斗结束边（接受/兜底）会丢弃未消费的热键标志，不跨场残留。
- 设置面板打开时钩子不捕获这三枚热键。
- 单次接管绑定当前尝试中的 `side + army_slot_ix + creature_id`，松键不会丢锁定；真正消费以 `0x4786B0` 执行玩家动作为准。它是本轮人工接管的运行时校验，不是跨重打的配置身份。
- 单次接管、打开设置键只允许单个字母，禁止 F1–F12、数字、Ctrl、Alt、Tab、Shift；打开设置键另排除战斗占用集（§15.4）。
- 状态提示：`打铁助手: 自动 / 全手动 / 单次接管 / 单次待命`。

否则按单位类型判定：

| 单位    | 判定                        |
| ----- | ------------------------- |
| 弹药车   | 保持原版                      |
| 急救帐篷  | 配了急救→执行；否则有急救术→原版，无→交 AI  |
| 投石车   | 防御→代为防御；其余保持原版（手动） |
| 弩车/箭塔 | 远程→执行；有炮术且手动→原版；其余交 AI |
| 普通部队  | 手动→保持原版；否则按配置执行（绝不交回 AI） |

### 10.2 动作提交（`TrySubmitConfiguredAction_`）

按 `rule.action` 分发到 `Submit*`；普通部队失败且允许时降级 `SubmitDefend_`：

- **防御** `SubmitDefend_`：`action=BA_DEFEND`。
- **等待** `SubmitWait_`：`action=BA_WAIT`。
- **循环移动** `SubmitMove_`：按 `moveWaypoints` + `moveWaypointCursor` 逐点巡逻，末点回首点；未配置路径时保留旧单目标兜底。
- **循环近战** `SubmitMelee_`：读取当前 `meleePairCursor` 组合；攻击格上有敌人才出手；`move_type=7`，`action=BA_WALK_ATTACK`，param=站立格，target=攻击格；成功提交后游标推进，失败不推进。
- **远程** `SubmitRanged_`：`SelectStackTarget_` 选敌方目标，`action=BA_SHOOT`，`actionTarget=目标格`。
- **急救** `SubmitFirstAid_`：选己方伤员，`action=BA_FIRST_AID`，`actionParameter=目标格`。
写入后原版主循环（`FUN_004786b0` 等）接管动画 / 伤害 / 回合推进。

### 10.3 目标选择（`SelectStackTarget_`）

先按 `side` 过滤双方 `stack[2][21]` 得候选，再按选择器挑一个：

- `SEL_RANDOM`：随机候选。
- `SEL_RANGED_SPEED`：远程优先，其次飞兵，同类再按速度降序。
- `SEL_COUNT_HIGH`：存活数量最多。
- `SEL_WOUND_RATIO`：失血比例最高（`失血数值 / (开战数量×单兵满血)`）。
- `SEL_WOUND_VALUE`：失血数值最大（阵亡单位满血 + 当前顶层已损 HP）。

### 10.4 快捷施法执行

**实现**：UI 为 `spellSlots[10]` + `spellSlotCount`（兼容旧 `spellSlot` / `quickCastFirst`）；运行游标在跟踪表 `spell_cursor`。执行端即单位行动管线（`g_pipeline_stage`，§15.2）：

1. `PS_IDLE`：控制权交给玩家时，若序列非空、管线空闲、本回合该单位未施过，投递快捷键 `1-9/0` → `PS_SPELL_POSTED`（本帧不提交部队动作）；
2. `PS_SPELL_POSTED`：观察 `heroCasted` 变化 / 法力下降 / 超时（2000ms 按真实时间）→ 推进游标、清投递 → `PS_SPELL_DONE`（无论成败都不重投）；
3. `PS_SPELL_DONE`：施法阶段结束，提交该部队主动作（主动作为手动时仅施法后交回玩家）→ `PS_HANDLED`；
4. `PS_HANDLED`：本回合已处理（含降级失败交回玩家），不再重复下命令；活动单位变化时管线整体回 `PS_IDLE`。

管线锚定 `g_pipeline_stack`（活动单位）；英雄本回合已施过法时直接进 `PS_SPELL_DONE` 跳过投键。

保活插在管线之前。判定时机按策略分派（§3.1.1）：「回合内首动」用战场 `turn`（`H3CombatManager +0x13D70`）每回合只判一次；「部队全灭后」「损失量大于恢复量」每次行动都判，已施法（`hero_casted`）或法力不足时静默跳过：

1. 遍历勾选了「加入保活队列」的槽（跟踪表校验人类侧身份）：按其生物 `undead` 标志定法术（聚灵 39 / 复活 38），取该法术当前等级的可恢复量（基础 50×力量、高级 75×力量、专家 100×力量，以逆向结论为准），按方案级策略判定该部队是否够格：无=不救；部队全灭后=仅已全灭且有尸体者；回合内首动=已损 HP>0 即够格；损失量大于恢复量=已损血量（死亡数×满血＋顶层已损，全灭即满损失）严格大于可恢复量。够格者入候选。
2. 英雄已学该法术、法力足够且本回合未施法：调 `H3CombatManager::CastSpell`（`0x5A0140`，`cast_type` 实参 0=单体施法，语义为逆向推断）直接施放（目标=该部队所在格），不占快捷施法、不推进任何部队的循环施法游标。
3. 条件不满足、无人勾选、法术没学、法力不足或英雄已施法：本回合记为已判，直接进入上面的循环施法。
4. 若当前部队主动作为手动：保活施法结束后交回玩家，与「只配了循环施法」相同。

保活与自动停止的入口守卫：仅「战斗中·面板关 + 控制权为自动」时判定（§3.1.1/§3.1.2）。

---

## 11. 待完善清单

1. **状态机上机回归**（代码已完成，见 §15）：打开面板即停自动、P 键打开、结算接受后下一场恢复自动、重打保留全手动、战斗中/结算中读档不残留跟踪、3000ms 重打宽限不误触发。
2. **存档修复上机回归**：点存档不再崩溃；旧 `H3Auto.profiles`（缺陷版本写出）读档失败属预期，需重新配置。
3. **循环施法运行验证**：执行端已接入两阶段快捷键投递/结果等待（§10.4 管线）；需验证各类法术（立即施放、需要选目标、无效快捷键、法力不足）的 `heroCasted`/法力变化和超时行为。
4. **移动寻路可达性校验**：提交循环移动前已调用原版目标判定并检查 `accessibleSquares2`；仍需游戏内回归验证边界地形、双格部队和不可达目标的降级行为。
5. **降级语义边界**：未允许降级且配置动作失败时交回玩家；战争机器须具备对应技能，无技能时保持原有分支行为。仍需游戏内回归各动作失败场景。
6. **设置清除时机**：已实现结果窗接受清除 / 取消重打保留 / 兜底边（§9.1）；需游戏内回归（含 HD ReplayableQB 与纯原版结果窗）。
7. **诊断日志清理**：定位保存崩溃用的分段诊断日志（打开阶段、s_p、编码 n、异常 at 等）仍在源码里，用户上机确认修复后删除。
8. **战争机器卡布局打磨（可选）**：当前已隐藏降级、按技能分流行动；若需再隐藏循环施法行或改成更紧凑布局，另开需求。

---

## 12. Hook 列表（实测以代码为准）

| 地址        | 类型     | 用途                        |
| --------- | ------ | ------------------------- |
| `0x600430` | LoHook | BltComplete：面板绘制 / 施法等待推进 / 热键轮询 / 状态机事件源（UI 出现·消失·P 键消费） |
| `0x4746B0` | LoHook | 战斗消息入口：面板输入屏蔽 / 提交部队主动作 |
| `0x4744D0` | HiHook | 是否自动执行判定（战争机器 AI / 人工接管门 / 管线残留清理） |
| `0x4786B0` | HiHook | 原版动作执行入口（单次接管完成确认） |
| WH_KEYBOARD | 线程钩子 | 常驻键盘钩子：F9/J/P 热键与数字录入捕获（keydown 边沿） |
| WH_MOUSE | 线程钩子 | 面板鼠标：下拉悬停 / 屏蔽战斗 hover |
| `+0x132D4` | 字段     | `H3CombatManager::mouseCoord`（拾取当前格） |
| `0x493FC0` | 调用     | 战场重绘（撤销临时格子标示）              |
| `0x0063D46C` | 虚表   | 原版 CPResult 结果窗（生命周期检测） |
| `0x7802` / `0x1FB` | 控件 id | 结果窗确定 / HD 取消重打 |

> 具体挂接以 `modules/Entry.inc.cpp` 与各模块实际代码为准；地址如与实测不符，以代码注释为准。

---

## 13. 文件结构

```
H3Auto/
├── H3Auto.cpp               # 主入口
├── H3Auto.vcxproj
├── H3Auto.ini               # 静态配置（UI 标签 / 热键）
├── H3Auto.rc                # 版本信息
├── build_and_deploy.bat     # 编译 + 部署（统一入口）
├── 设计文档.md
├── img/
│   ├── HA_bg.pcx            # 面板背景
│   ├── HA_cell.pcx          # 卡片背景 568×110（随卡片尺寸重生）
│   ├── HA_grid_frame.pcx    # 网格金框 624×342
│   ├── HA_icon_frame.pcx    # 图标金框（60×66）
│   ├── HA_ok_*.pcx / HA_cancel_*.pcx / HA_button_frame.pcx
│   └── …
├── tools/
│   ├── gen_cell_frame.js    # 生成卡片背景（传宽高）
│   ├── gen_grid_frame.js
│   └── gen_icon_frame.js    # 生成图标金框
└── modules/
    ├── ConfigLog.inc.cpp    # 枚举 / 结构 / 方案存储 / 日志
    ├── Compat.inc.cpp       # 结构体与内存访问兼容层
    ├── Entry.inc.cpp        # Hook 挂接与入口
    ├── SettingsDlg.inc.cpp  # 设置面板 / 网格 / 滚动条 / 战场拾取
    ├── CellControl.inc.cpp  # 卡片控件绘制 / 命中 / 下拉几何
    └── AutoExecute.inc.cpp  # 接管判定 / 动作提交 / 目标选择
```

---

## 14. 构建与部署

- **统一入口**：`build_and_deploy.bat`（编译 + 部署一步完成，不要单独跑 `build.bat` / `deploy.bat`）。
- 源文件 UTF-8，中文游戏字体走 GBK；换行 CRLF。
- 资源尺寸严格校验：卡片 / 金框尺寸改动后必须用 `tools/gen_*.js` 重生对应 PCX，否则背景加载失败。
- DLL 在游戏进程启动时加载，改动后必须**完全重启游戏**才生效。

---

## 15. 战场状态机（已实现）

插件的控制流以三层状态机组织：顶层战场生命周期（本节 15.1）、控制权子状态与单位行动管线子状态（15.2）。所有迁移走唯一出口 `SetPhase_(next, ev)`（实现在 `modules/BattleState.inc.cpp`，类型与声明在 `modules/BattleState.hpp`）：合法边查表 → 边动作 `PhaseEdgeAction_(from, ev)` → 写 `g_phase` → `[Phase]` 日志；非法组合记日志并保持原状，幂等（同状态重复事件不算迁移）。

### 15.1 顶层状态与迁移

```cpp
enum BattlePhase {
    BP_PEACE,          // 战斗前
    BP_COMBAT_CLOSED,  // 战斗中·面板关
    BP_COMBAT_OPEN,    // 战斗中·面板开
    BP_RESULT,         // 战斗结算
    BP_ENDED,          // 战斗结束（瞬态：进入即清理，随即续转 BP_PEACE）
};
```

合法迁移边（查表）：

```text
战斗前 ──战斗UI出现──> 战斗中·面板关
战斗前 ──结果窗出现──> 战斗结算           （快速战斗：从未出现战斗 UI）
战斗中·面板关 ──右键自动战斗/P键──> 战斗中·面板开   边动作：强制停（15.3）
战斗中·面板开 ──确定──> 战斗中·面板关    （提交方案；已停，等 F9）
战斗中·面板开 ──取消/ESC──> 战斗中·面板关 （丢弃草稿；已停，等 F9）
战斗中·面板关/面板开 ──结果窗出现──> 战斗结算（面板开时强制关面板）
战斗结算 ──取消/重打──> 战斗中·面板关    边动作：重排+重绑+清运行时，方案保留，置 3000ms 重打宽限
战斗结算 ──接受──> 战斗结束              边动作：清方案+运行时+跟踪，控制权回自动
战斗中·面板关/面板开/结算 ──UI消失且未经结算──> 战斗结束（兜底边，方案保留，控制权回自动）
战斗结束 ──（瞬态续转，沿用进入事件）──> 战斗前
```

- **战斗结束是瞬态**：无驻留时间、无自己的事件，进入即清理后自动续转「战斗前」；保留为独立状态是为了清理动作有归属、日志可读。续转边（`from == BP_ENDED`）不执行任何边动作——清理只在进入 ENDED 的主边做一次，不依赖边动作幂等。
- **事件源**：BltComplete 每帧扫描——战斗 UI 出现（虚表判定）、UI 连续缺失 ≥3 帧（兜底边；重打宽限 `g_ui_gone_grace_until` 3000ms 内不触发，防 UI 重建误触）、P 键标志消费；`CheckBattleResultLifecycle_`——CPResult 结果窗出现/接受/取消（归并成 `BE_RESULT_SHOWN / ACCEPTED / RETRY` 三事件）；面板鼠标钩子与消息入口——面板开关。
- **守卫不进顶层**（满足外部形态与行为变化但无进出清理动作）：战术布阵（tactics）、隐藏战斗（`0x46A080`：快速战斗/AI 对打/回放）、原版 `auto_combat`、游戏非前台。这些在执行链入口以条件判定挡住。
- 旧散落判断的归并：`IsPanelActive` 等价于 `BP_COMBAT_OPEN`（面板真实开态仍查 `s_p.active`）；执行/保活/自动停止入口统一守卫 `g_phase == BP_COMBAT_CLOSED`。

### 15.2 子状态（战斗中·面板关内）

**控制权 `g_control`**（取代旧 battle_manual / oneshot_active / oneshot_pending 三标志）：

| 模式 | 含义 | 进入 | 离开 |
| --- | --- | --- | --- |
| `CM_AUTO` | 自动执行 | 默认；单次接管完成/过期；接受结算；兜底边 | F9 切换、J 锁定、打开面板、保存方案、自动停止触发 |
| `CM_MANUAL` | 本场全手动 | F9、打开面板边（15.3）、保存方案、自动停止触发 | 再按 F9；接受结算/兜底边回自动；**重打保留** |
| `CM_ONESHOT_WAIT` | 单次接管待命（无可锁部队或有飞行中动作） | J 按下且暂不可锁 | 下一支可锁部队出现→LOCKED；切 MANUAL/AUTO 清 |
| `CM_ONESHOT_LOCKED` | 单次接管锁定中 | 待命的部队出现，或 J 时恰有可锁部队 | 锁定部队完成一次玩家动作（`0x4786B0` 确认）→AUTO；活动单位已变→AUTO |

用户可见切换走 `SetControlMode_`（日志 `[Control] control=…` + 状态提示刷新）；自动停止触发用直接赋值（自带中文日志）。锁定目标数据（side/slot/creature/指针）仍在 `g_auto_state`，`ClearOneShotManual_` 只清数据，模式由调用方归位。

**单位行动管线 `g_pipeline_stage`**（取代旧 spell_waiting / spell_wait_stack / spell_done_stack / last_handled_stack 四标志；详见 §10.4）：

| 阶段 | 含义 |
| --- | --- |
| `PS_IDLE` | 当前单位无未完成管线 |
| `PS_SPELL_POSTED` | 已投递快捷施法键，等待结果（超时数据在 `g_auto_state.spell_wait_*`） |
| `PS_SPELL_DONE` | 本单位本回合施法阶段已结束，不再投键 |
| `PS_HANDLED` | 本单位本回合已处理（已下命令或交回玩家），不再重复处理 |

管线锚定 `g_pipeline_stack`（活动单位）；「活动单位变化→整体回 IDLE」是唯一集中清理点（BltComplete 与 0x4744D0 两处）。`ClearSpellWait_`（撤销投递）把 POSTED→DONE 且保留锚定单位，防同回合重投；彻底清零由 `ResetAutoState` 与单次接管清单位负责。`action_wake_stack`（投递唤醒消息等待提交）保留原样——它是投递细节而非回合状态。

**面板开内的子模态**（帮助弹窗、快捷键录入、近战拾取隐藏态、停止数字录入）不入状态机，由面板自身的模态计数器管理（§8.0）。

### 15.3 打开边的强制停（关键决策）

进入「战斗中·面板开」的边上**必须已停止自动执行**——不做前置拒绝，而是打开同帧强制：

1. 切全手动：`SetControlMode_(CM_MANUAL)`（与 F9 同一条清理路径）+ `ClearOneShotManual_()`；
2. 清施法等待：`ClearSpellWait_()`（`GetTickCount` 相对超时挂在面板关闭后会瞬间误超时，故清空而非冻结；管线 POSTED→DONE，该单位本回合不再重投）；
3. 再开面板：右键路径与 P 键路径都是先 `OpenSettingsPanel_()`、`s_p.active` 置位成功后才 `SetPhase_(BP_COMBAT_OPEN, BE_PANEL_OPEN_REQUESTED)`——杜绝「已迁移但面板没开」的死状态。

连带简化：面板「确定」的 `CommitProfiles → Pause → Close` 中 `Pause` 退化为幂等保护。唤醒等待（已投递的鼠标移动）无害可保留；单次接管随切停清除，属有意行为（改完设置本就要 F9 重启）。切停前一帧已投出的快捷键消息仍会到达，面板开时 `Hook_BattleMsgProc` 吞数字键已覆盖。全手动下战斗仍会推进到结算，「面板开 → 结算」边保留。

### 15.4 打开设置热键

- INI `[Hotkeys] OpenSettings=P`（默认）；解析复用 `ParseHotkeyVk_`（letter_only），校验 A–Z 且排除战斗占用集 `A C D E H I L O Q R S T W`（原版/HD/HotA 战斗键）+ 本插件 `J`，与 `OneShotManual` 相同时也回退 P，记日志。
- 处理链与 F9 同构：常驻键盘钩子只置 `kb_open_panel_seen` 标志（面板开时不捕获），BltComplete 的「战斗中·面板关」分支消费；消费即走 15.3 打开边（与右键同一条边动作）。
- 战斗结束边（接受/兜底）丢弃未消费的标志，不跨场残留；帮助弹窗热键行从当前配置生成三键说明。
