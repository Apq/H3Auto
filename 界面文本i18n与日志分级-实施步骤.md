# 界面文本 i18n 外置与日志分级 — 实施步骤

## 0. 需求与用户决策

三条需求：
1. 界面上显示的所有文本内容（含颜色）从配置文件加载，不写死在代码中。
2. 「行动前循环施法」改名「行动前循环快捷施法」。
3. 日志分级（常见级别）；帮助界面加下拉框指定最小记录级别（默认 info 以上）；加按钮打包该插件最后 5 个日志文件为 zip，复制 zip 路径到剪贴板，提示玩家到 QQ 粘贴/发送到群或加 712999712 私发。

用户澄清决策（2026-07-11）：
- **H3Auto.ini 改成 UTF-8 带 BOM**；文本放**另一个专用 ini**，按 **i18n 常见目录结构**组织，方便以后做多语言翻译。
- 初始配置内容由**项目提供**（仓库内模板随部署脚本分发）。
- 打包日志 = **生成 zip 并复制路径**（非合并文本）。
- 外置范围 = **全部界面文本**：设置面板、帮助模态、表格控件、下拉选项、tips、状态栏、战场 HUD 角标；动态数字用格式串（如 `方案 %d`）。

## 1. 现状

- **配置读取**：`ReadConfig()`（ConfigLog.inc.cpp:385）用 `GetPrivateProfileIntA/GetPrivateProfileStringA`（ANSI 接口）读 H3Auto.ini；`ReadDisableLogFromIniFileW`（同文件:112）已手写了一个 UTF-8 解析（仅取 [Logging] DisableLog）。仓库内 H3Auto.ini 实际为 UTF-8 无 BOM —— **ANSI 接口读 UTF-8 中文（Title=打铁设置、[Actions] 0=手动 等）按 GBK 误解，是现存乱码隐患**，本次改造一并修复。
- **已有外置文本**：`[Panel] Title`、`[Actions]`（0..6）、`[Selectors]`（0..2..4）经 `LoadLabels_`（SettingsDlg.inc.cpp:46）进 `g_action_labels/g_selector_labels/g_panel_title`。
- **写死的界面文本分布**：
  - PanelDraw.inc.cpp（18 处 DrawTxt）：帮助模态（标题/8 行/关闭）、tips 表 `kTips` 7 条、标题带热键 tip、读档/存档按钮、方案按钮、保活/停止行、法术键模态、近战标示。
  - CellControl.inc.cpp（15 处）：「允许降级为防御」「行动前循环施法:」「加入保活队列」标签、目标选项 3 条、「循环站立位+攻击位/己方伤员/无目标」、`CellControl_TipForHit` 8 条。
  - AutoExecute.inc.cpp：`ControlModeLabel_`（自动/全手动/单次接管/单次待命）+ HUD 前缀「打铁助手: %s」（RefreshControlStatusHint_）。
  - SettingsDlg.inc.cpp：状态栏 4 条（{绿}存/读档成功、{红}存/读档失败）。
- **日志**：`WriteLog(fmt,...)`（ConfigLog.inc.cpp:478）无级别；每次 DLL 加载新建 `H3Auto_YYYYMMDD_HHMMSS.log`（UTF-8 BOM）于 DLL 同目录，保留 30 个；`[Logging] DisableLog` 可全关。调用点约 40+ 处分布 AutoExecute/SettingsDlg/BattleState/Entry。
- **帮助模态**：480×336 居中（GetHelpModalRect_），标题 + 8 行×28 + 关闭按钮（92×26）。输入处理：`?` 按钮、关闭按钮命中在 SettingsDlg 点击逻辑中。
- **部署**：deploy.ps1 复制 Release\H3Auto.dll + 仓库 H3Auto.ini + img\ 到 `Packs\打铁助手\`（H3Auto.ini 目前强制覆盖）。

## 2. 设计

### 2.1 文件布局（i18n）

```
仓库/                          游戏目录 Packs\打铁助手\
  H3Auto.ini        ────────→   H3Auto.ini      （通用配置：热键/日志/语言）
  lang\zh-CN.ini    ────────→   lang\zh-CN.ini  （中文文案）
  lang\en-US.ini（预留，本次不出）
```

- H3Auto.ini：UTF-8 带 BOM。新增 `[General] Language=zh-CN`（缺省 zh-CN）、`[Logging] MinLevel=info`（缺省 info）。
- lang\<lang>.ini：UTF-8 带 BOM，`[节]` + `键 = 值`，`#`/`;` 注释；值内可含富文本颜色标记（{金} {#RRGGBB} 等）与 `%s/%d` 格式串。
- **回落链**：lang 文件缺键/缺文件/语言名非法 → 代码内置默认表（与 zh-CN.ini 内容镜像）。内置默认保证删光配置也不崩、不空串。
- **部署策略**：H3Auto.ini 与 lang\*.ini 部署时**均强制覆盖**（文案与配置模板随版本更新；玩家深度定制文案 = 自己另存 Language 副本，文档说明）。

### 2.2 键规划（lang/zh-CN.ini）

| 节 | 内容 | 示例键 |
|---|---|---|
| [panel] | 面板标题、按钮、行标签、保活策略下拉选项（现 PanelLayout.hpp PROTECT_STRATEGY_LABELS）、状态栏文案 | title、save、load、ok、cancel、protect_label、protect_opt0..3、stop_label、status_save_ok |
| [help] | 帮助模态全部行（open_line/hotkey_line 为格式串）、关闭、新增日志级别行与打包按钮、打包结果提示（含 QQ 号） | title、line_open（`打开设置：…%s…`）、line_hotkey、close、log_level_label、log_level_opt0..4、pack_btn、pack_ok、pack_fail |
| [tips] | 标题带热键 tip（格式串）、kTips 7 条、表格内 8 条 | titlebar、btn_load、btn_save、btn_profile、protect_row、stop_row、btn_ok、btn_cancel、cell_action、cell_selector、cell_fallback、cell_protect、cell_drop、cell_spell、cell_move、cell_melee |
| [cell] | 格子控件标签与目标选项、法术键模态全部文本 | allow_fallback、pre_cast_label（**行动前循环快捷施法:**）、protect_join、target_melee_pair、target_wounded、target_none、spell_modal_title（设置快捷施法键）、spell_modal_slot（格式串 `正在设置循环施法第 %d 槽`）、spell_modal_hint1、spell_modal_hint2、spell_modal_cancel、melee_stand/melee_attack（近战标示） |
| [hud] | 战场 HUD 角标 | prefix（`打铁助手: %s`）、mode_auto、mode_manual、mode_oneshot、mode_wait |
| [actions] | 行动下拉标签（沿用现 [Actions] 0..6 语义） | 0=手动 … 6=急救治疗 |
| [selectors] | 目标选择器标签（沿用现 [Selectors]） | 0=随机 … |

- 原 H3Auto.ini 的 `[Panel]/[Actions]/[Selectors]` 迁到 lang 文件；H3Auto.ini 只留机器配置（General/Hotkeys/Logging）。迁移后旧键不再读（部署模板整体更新）。
- 颜色标记规则不变：{颜色名}/{#RRGGBB}/{0xRRGGBB}/{rgb(r,g,b)}/{XX}。纯文本位置（非状态栏）的键若带标记会原样显示，文档注明只有 [panel] status_* 与 [help] pack_* 走富文本状态栏。

### 2.3 UTF-8 ini 读写器（新 modules/IniUtf8.inc.cpp）

- `IniReadUtf8(path, section, key, default, out, out_size) -> bool`：逐行解析，跳 BOM/`;`/`#` 注释、`[节]` 切换；行内首个 `=` 分键值；Trim 两侧空白；节/键比较区分大小写不敏感（`_stricmp`）。整文件读入堆缓冲（≤64KB）。
- `IniReadIntUtf8(path, section, key, default) -> int`。
- `IniWriteKeyUtf8(path, section, key, value) -> bool`：读入→逐行找 `节/键` 替换值；找不到则插入到节尾或追加新节；写回时统一 UTF-8 **带 BOM**，保留其余行与注释。
- `ReadConfig()` 的 `GetPrivateProfileIntA/StringA`、`LoadLabels_`、`ReadDisableLogFromIniFileW` 全部改走上述读写器（删除 W 版重复实现）。读兼容无 BOM 旧文件。

### 2.4 UiText 模块（新 modules/UiTexts.inc.cpp）

- 内置默认表：`struct UiTextDefault { const char* key; const char* text; }`，key 用 `panel.title` 全名（含点）。
- 启动 `LoadUiTexts()`（Entry DllMain/初始化处调用一次）：从 `<dll 目录>\lang\<Language>.ini` 读，命中则拷入平行覆盖缓冲 `char overrides[N][256]`。
- `T(key) -> const char*`：查覆盖缓冲 → 内置默认 → 断言键不存在时返回 `"!!key!!"`（可立即发现漏键）。
- include 顺序调整：`IniUtf8 → ConfigLog → UiTexts → Compat → PanelGfx → AutoExecute → BattleState → CellControl → SettingsDlg → PanelInput → PanelDraw → Entry`（IniUtf8 为纯解析不依赖 WriteLog，必须排在 ConfigLog 的 ReadConfig 之前；UiTexts 需在最早使用者 AutoExecute/HUD 之前，且依赖 ConfigLog 的路径与日志）。
- `g_action_labels/g_selector_labels` 数组接口保留（CellControl 按枚举序号索引），但改由 LoadUiTexts 解析 `actions.0..N`/`selectors.0..N` 直接填充，删除 LoadLabels_ 与 ini 读取。

### 2.5 日志分级

- 级别：`LOG_TRACE < LOG_DEBUG < LOG_INFO < LOG_WARN < LOG_ERROR`（常见五级）。
- `WriteLog` 改为 `WriteLogLv(int level, fmt, ...)`；保留 `WriteLog(fmt,...)` 作为 INFO 别名以减少机械改动？——**不留别名**，全部调用点显式换 `LogInfo/LogDebug/LogWarn/LogError`（四个 static 函数），避免新旧混用。
- 行格式：`[时间] [INFO] 消息`。低于 `g_log_level` 的不落盘（DisableLog 仍最高优先）。
- 定级标准：配置加载/方案存读/面板开关/状态切换 = INFO；四轮匹配细节、逐步执行轨迹、拾取坐标 = DEBUG；配置回退/非法值 = WARN；SEH 捕获/初始化失败 = ERROR。
- `g_log_level` 从 `[Logging] MinLevel` 读（trace/debug/info/warn/error，不区分大小写，坏值回 info）；帮助界面下拉改动后 `IniWriteKeyUtf8` 写回，立即生效。

### 2.6 帮助模态扩展

- 尺寸 480×336 → 480×**412**：原 8 行下方加两行控件区（y 偏移按 28 行距）：
  - 行 A：标签「日志级别:」+ 下拉框（宽 ~120）：选项 `全部/调试/信息/警告/错误`（对应 TRACE..ERROR）。选中即改级别并写 ini，状态栏提示。
  - 行 B：按钮「打包日志」（宽 ~180×26）：执行打包。
- 下拉交互实现：帮助模态内自绘简易下拉（点开列出 5 项、再点收起），不复用 CellControl 下拉（那套绑定表格卡片 hit 区）。命中处理放 SettingsDlg 现有帮助模态点击逻辑旁。

### 2.7 日志打包 zip

- 查找：DLL 同目录 `H3Auto_*.log`，按 LastWriteTime 降序取前 5（含当前会话日志）。
- zip：**手写 STORE 法 zip 写出器**（纯 C：local file header + data + central directory + EOCD；CRC32 查表 256 项；DOS 时间戳取本地时间）。不压缩，日志为 UTF-8 文本，几十 KB~几 MB 可接受；避免引入 zlib 依赖。文件名 `H3Auto_logs_YYYYMMDD_HHMMSS.zip`，放 DLL 同目录（与日志一致，玩家好找）。zip 内条目名即日志文件名（ASCII，无中文路径问题）。
- 剪贴板：`OpenClipboard(nullptr)` 失败重试 ≤5 次（游戏内可能被占用）；`EmptyClipboard + SetClipboardData(CF_TEXT, GlobalLock 缓冲)`（路径 ASCII，CF_TEXT 足够；失败再试 CF_UNICODETEXT 不必）。整体 SEH 不包（全 Win32 API，无野指针风险点；CRC/写文件循环纯内存）。
- 成功：状态栏富文本提示（[help] pack_ok，含 zip 文件名 `%s` 与 QQ 号 712999712）；失败（无日志/写 zip 失败/剪贴板占用）：pack_fail 同理。
- 「复制的是 zip 完整路径」：提示文案写清楚「路径已复制，到 QQ 聊天框粘贴后回车发送，或直接把 zip 文件拖进 QQ；可加 712999712 私发」。

### 2.8 部署脚本

- deploy.ps1：H3Auto.ini 改为**存在则不覆盖**；新增复制 `lang\*.ini` → `$packsDst\lang\`（覆盖）。

## 3. 实施步骤（每步编译 0 error 才进下一步）

1. **IniUtf8 读写器**：新模块 + ReadConfig/LoadLabels_/DisableLog 切换 + 删 W 版旧解析；ini 模板转 UTF-8 BOM。
2. **UiTexts 模块**：默认表（含改名「行动前循环快捷施法:」）+ T() + LoadUiTexts + H3Auto.ini 加 Language；include 顺序调整；vcxproj 加新文件。
3. **调用点替换**：PanelDraw/CellControl/AutoExecute(HUD)/SettingsDlg 全部字面量 → T()；[Actions]/[Selectors]/Title 迁 lang；删除 LoadLabels_ 旧机制（g_action_labels 改由 UiTexts 提供，CellControl 引用点同步）。
4. **日志分级**：LogTrace/Debug/Info/Warn/Error + g_log_level + MinLevel 读取 + 全部 WriteLog 调用点定级改名 + 行前缀。
5. **帮助模态扩展**：尺寸/两行控件绘制 + 下拉交互 + 级别写回 ini + 状态栏提示。
6. **zip 打包 + 剪贴板**：写出器 + 按钮逻辑 + 提示文案。
7. **部署与文档**：deploy.ps1 调整、lang\zh-CN.ini 模板定稿、设计文档.md §8.6 与新章节、README（若有）。
8. **回归**：Rebuild 0 error + bt.bat 单测全过 + 本文档检查记录节填写。

## 4. 验收标准

- 删除 lang 目录后界面仍完整显示（内置默认）；改 lang\zh-CN.ini 任一键（含颜色标记）重启生效。
- H3Auto.ini（UTF-8 BOM）中文注释/值读取正确；MinLevel 修改写回后重启沿用。
- 帮助界面下拉切换级别后，低级别日志立即不再落盘。
- 「打包日志」在存在 ≥1 个日志时产出 zip 且路径在剪贴板（记事本可粘贴出路径）。
- 界面无「!!…!!」漏键占位。
- MSBuild Release Win32 0 error；PolicyCoreTests 全过。

## 5. 检查记录（自查与最终检查，实施时填写）

- [x] 自查 round 1（文档完稿后）：键清单 vs 代码字面量逐一对照；include 顺序修正为 IniUtf8 最前（ReadConfig 依赖）；SEH 边界合规（打包/剪贴板纯 Win32 不包 SEH）；zip 与剪贴板均 ASCII 路径无中文坑；补齐法术键模态与保活策略标签键。发现并修正：CC_COL2_W 118 放不下新标签，联动调整 CC_COL2_W=132、CC_COL3_RIGHT=564、施法槽 34→30、移动槽 43→40、近战槽 71→66，三组槽位容量保持 10/16/10 不变。
- [x] 步骤 1（IniUtf8）：Rebuild 0 error，DLL 17:47:47 / 126464。ReadConfig/LoadLabels_/DisableLog 全部切换，W 版旧解析删除，ini 模板转 UTF-8 BOM。
- [x] 步骤 2（UiTexts）：0 error，DLL 17:50:37 / 130560。g_action_labels 等定义迁入 UiTexts，Entry 改调 LoadUiTexts。
- [x] 步骤 3（调用点替换）：0 error，DLL 17:55:19 / 130560。PanelDraw/CellControl/AutoExecute(HUD)/SettingsDlg 全部字面量换 T()；PanelLayout 的 PROTECT_STRATEGY_LABELS 删除。
- [x] 步骤 4（日志分级）：0 error，DLL 17:58:25 / 131072。约 140 处调用点按内容定级（failed/异常=error，跳过/回转=warn，轨迹=debug）；修 patch 工具把 "\r\n" 拆断的字面量（改用 Python 修）。MinLevel 解析加入 ReadConfig；日志枚举/全局前移到 ReadConfig 之前。
- [x] 步骤 5+6（帮助模态+打包）：0 error，DLL 18:03:02 / 138752。HelpLogLevelOptKey_ 一度插进函数体内（C2267/C2601）移出；GetHelpLogLevel* 三函数补进 PanelDraw.hpp（SettingsDlg 在 PanelDraw 之前 include）。zip 用 STORE 手写结构，total 大小改为循环累计。
- [x] 步骤 7（部署与文档）：deploy.ps1 加 lang 覆盖复制（ini/lang 均强制覆盖，按用户 2026-07-11 指示）；lang/zh-CN.ini 从 kUiTextDefaults 生成（69 键+actions/selectors）；H3Auto.ini 删迁走三节；设计文档全文改名+新增 §16。
- [x] 步骤 8（回归）：PolicyCoreTests 142 checks passed；界面漏键检查：T() 未命中返回 "!!键不存在!!"，代码内全部调用键均在默认表中。
- [x] 反馈修正（2026-07-11 用户上机三条）：①剪贴板改 CF_HDROP 文件式复制（DROPFILES 头 shellapi.h 在此工具链不可靠，自写 20 字节布局）+CF_TEXT 附路径，QQ 可直接粘贴发送 zip；②pack_ok 文案带全 QQ 号（群 1042362808/740338251、私 712999712），UiTexts 默认表与 lang/zh-CN.ini 同步；③状态栏 bug 根因=悬停 tips 每帧刷新覆盖结果消息，SetStatusText_ 加 sticky 参数 + s_status_priority_until 保护期，存/读档/打包/级别四处结果消息 sticky=true，PanelDraw tips 刷新先查 StatusResultHoldActive_。顺带纠级别误判（存/读档结果行 LogError→LogInfo，悬停屏蔽失败→Warn）。验收：0 error，DLL 18:21:34 / 139264；PolicyCoreTests 142 checks passed。
- [x] 反馈修正 round 2（2026-07-11）：①ini 模板 [Hotkeys] 补 OpenSettings=P（此前漏列，代码默认 P 兜底但配置文件不可见）；②状态栏接口按用户定语义改为纯覆盖+重新计时，撤 sticky 优先级（删 s_status_priority_until/StatusResultHoldActive_），tips 改为光标移动才触发（s_tip_last_cx/cy），静止悬停仅对 tip 续期、结果消息自然到期；③根因修正：PanelTipAt_ 命中顺序 按钮表 提到 标题带 之前——读档/存档按钮在标题行（HELP_BTN_Y），全宽标题带矩形（680×44）劫持了按钮悬停，点存/读档后每帧刷新的是热键 tip 而非按钮 tip/结果文字；④帮助模态打开期间面板 tips 停用（防穿透错位提示）。验收：0 error，DLL 18:31:49 / 139264；PolicyCoreTests 142 checks passed。
- [x] 反馈修正 round 3（2026-07-11）：①titlebar tip 触发范围收紧到标题文字附近（居中估算宽，非整条 680×44）；②打包卡死根因=全量读日志（≤64MB/文件×5）在 32 位游戏 UI 线程同步分配数百 MB → 换页假死，改为每文件只取尾部 2MB（SetFilePointerEx 跳前）+ new(std::nothrow) 分配失败检查；zip 字节布局等价性自检（Python 同规则构造 → zipfile 校验 CRC/内容，含 0 字节与奇数尺寸）通过。验收：0 error，DLL 18:39:32 / 140288；PolicyCoreTests 142 checks passed。
- [x] 打包改 LZMA .7z（2026-07-11，用户要求 7z 压缩）：LZMA SDK（7-Zip 官方，public domain）源码级集成于 lzma/（LzmaEnc.c/LzFind.c/CpuArch.c + 头文件，vcxproj 加 Z7_ST 关多线程匹配器）；.7z 容器手写（明文 header、每文件独立 folder）。容器布局先用 Python 探针调通（7z.exe 与 py7zr 双过），再翻译 C++。实跑验证：32 位探针（直接 include LogPack.inc.cpp）产出 .7z，7-Zip 官方 `Everything is Ok`（Method=LZMA:20，Blocks=3），py7zr 解压内容与原文逐字节一致（含 2MB 尾部截断）。坑：7zTypes.h 的 #endif 在 EXTERN_C_END 之后（官方笔误，include guard 命中时 extern "C" 不闭合）已修；StartHeader CRC 必须对 20 字节小端布局计算。验收：DLL 0 error（19:30:54 / 164352）；PolicyCoreTests 142 checks passed。
- [ ] 最终检查 round 2（上机后）：删 lang 后界面完整；改 lang 文案重启生效；UTF-8 BOM 中文读写正确；级别切换立即生效且重启沿用；QQ 聊天框 Ctrl+V 直接发送 zip 文件；结果消息期间悬停不再覆盖；无 !!…!! 占位。待部署 build_and_deploy.bat 后逐条过。
